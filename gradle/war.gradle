apply plugin: "war"

bootWar {
    mainClassName = "id.tech.cakra.ecfregistersvc.EcfregistersvcApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
